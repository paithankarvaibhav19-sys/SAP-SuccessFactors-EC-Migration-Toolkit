# Technical Design Document: Corporate Structure & Foundation Object Design

**Project Name:** GlobalTech Solutions - Legacy HRMS to SAP EC Migration  
**Document ID:** TDD-EC-001  
**Module:** Employee Central (Core)  
**Author:** VPR11 | Vaibhav Paithankar  
**Status:** Baseline Specification  

---

## 1. Business Objective & Scenario

GlobalTech Solutions is migrating from a legacy HRMS to SAP SuccessFactors Employee Central. This specification establishes the organizational data model, segregates Generic Objects (MDF) from Classic Foundation Objects (FO), and defines field-level cascading association rules to prevent invalid organizational data entry across global offices.

---

## 2. Organizational Hierarchy Architecture

The corporate structure enforces a strict 4-tier hierarchy. Lower-level objects inherit access and filtering context from their immediate parent entity.

```text
[ Legal Entity (GT_US / GT_IN) ]
    │
    └── [ Business Unit (BU_ENT / BU_CLOUD) ]
            │
            └── [ Division (DIV_HCM / DIV_INFRA) ]
                    │
                    └── [ Department (DEP_L2_SUPP / DEP_ENG) ]



## 3. Object Classification: FO vs. MDF Generic Objects

| Structural Object | Framework Type | Management Tool | Config Location |
| :--- | :--- | :--- | :--- |
| **Legal Entity (Company)** | MDF Generic Object | Manage Data | Configure Object Definitions |
| **Business Unit** | MDF Generic Object | Manage Data | Configure Object Definitions |
| **Division** | MDF Generic Object | Manage Data | Configure Object Definitions |
| **Department** | MDF Generic Object | Manage Data | Configure Object Definitions |
| **Cost Center** | MDF Generic Object | Manage Data | Configure Object Definitions |
| **Location** | Classic Foundation Object | Manage Org, Pay and Job Structures | Corporate Data Model (XML) / BCUI |
| **Location Group** | Classic Foundation Object | Manage Org, Pay and Job Structures | Corporate Data Model (XML) |
| **Geo Zone** | Classic Foundation Object | Manage Org, Pay and Job Structures | Corporate Data Model (XML) |

---

## 4. Association & Relationship Mapping

Associations establish directional parent-child relationships and enforce cascading dropdowns across the UI.

### 4.1 Hierarchy Association Matrix

| Parent Object (Source) | Child Object (Target) | Association Name | Association Type | Multiplicity |
| :--- | :--- | :--- | :--- | :--- |
| `LegalEntity` | `BusinessUnit` | `toBusinessUnit` | Valid When | 1 : N |
| `BusinessUnit` | `Division` | `toDivision` | Valid When | 1 : N |
| `Division` | `Department` | `toDepartment` | Composite | 1 : N |
| `Department` | `CostCenter` | `toCostCenter` | Valid When | 1 : 1 |

* **Valid When:** The child object exists independently in the database but is conditionally filtered based on the selected parent in Job Information.
* **Composite:** The child object's lifecycle is directly bound to the parent entity.


## 5. Object Definitions & Configuration Snippets

### 5.1 MDF Object Definition: Business Unit (`cust_BusinessUnit`)

```json
{
  "code": "cust_BusinessUnit",
  "effectivedating": "fromStartOfDay",
  "status": "active",
  "fields": [
    { "name": "externalCode", "type": "String", "maxLength": 32, "required": true },
    { "name": "name", "type": "Translatable", "required": true },
    { "name": "description", "type": "String", "maxLength": 255, "required": false },
    { "name": "headOfUnit", "type": "User", "required": false },
    { "name": "status", "type": "Generic", "validValues": ["A", "I"], "required": true }
  ],
  "associations": [
    {
      "name": "cust_toDivision",
      "targetObject": "cust_Division",
      "type": "Valid When",
      "multiplicity": "1:N"
    }
  ]
}



### 5.2.1 Key Implementation Notes: Corporate Data Model XML

* **Governance & Upload:** The Corporate Data Model (`corporate-data-model.xml`) governs Foundation Objects (FO) and is uploaded via **Provisioning** (`Provisioning > Managing Document Types > Succession Management > Import/Export Corporate Data Model`) or synchronized via **Business Configuration UI (BCUI)**.
* **Field Properties:**
  * `externalCode`: The immutable primary key (max-length 32) referenced during delta API extractions and data loads.
  * `status`: Enforces record lifecycle (`A` = Active, `I` = Inactive). Inactive records remain available for historical reporting but are hidden from front-end dropdowns.
  * `timezone`: Standardized Olson time zone identifier required for accurate Effective Dating calculations across distributed geographies.
* **MDF Migration Path:** While `Location` remains configured here as a Classic Foundation Object, standard organizational units (`LegalEntity`, `BusinessUnit`, `Division`, `Department`, `CostCenter`) are maintained as Generic Objects under the Metadata Framework (MDF) to enable rule-driven UI behavior and dynamic permissions.
* **Country-Specific Overrides:** Address format rules, postal code masks, and state picklists for locations in specific regions are declared in the companion file `country-specific-corporate-data-model.xml`.


## 6. Business Validation Rules

### Rule Logic: Enforce Active Cost Center on Department Assignment

* **Rule Trigger Hook:** `onChange` on `Department` field within `jobInfo`.
* **Condition:**
  ```text
  If Job Information.Department is NOT NULL


  Set Job Information.Cost Center = Job Information.Department.toCostCenter.costCenterCode
Set Job Information.Cost Center.Visibility = View (Read-Only)